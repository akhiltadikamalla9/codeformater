pipeline{
    agent any 
    stages {
    stage('Yaml file formatter') {
            steps {
                script {
		def yamllintPath = "${WORKSPACE}" 
		echo "${WORKSPACE}"
		}
                }
            }
        }
    }
