pipeline{
    agent any 
    stages {
    stage('Yaml file formatter') {
            steps {
                script {
	sh """
            # Find all YAML files in a specific directory
	    echo "${WORKSPACE}/codeformatter"
            
        """
			}
                }
            }
        }
    }
